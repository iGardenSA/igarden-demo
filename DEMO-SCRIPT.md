# DEMO-SCRIPT.md — iGarden Smart OS · Demo

> سكربت العرض الحي + النسخة المختصرة + Q&A الجاهز.
> بيئة العرض: `npm run dev` على اللابتوب · بلا إنترنت · المتصفح على `http://localhost:3210`.

---

## نسخة الـ 5 دقائق (الكاملة — للعروض التجريبيّة)

### ⏱ 00:00 – 00:30 — الافتتاح والتموضع
> "iGarden Smart OS — طبقة التشغيل والبيانات تحت المزارع المائية الذكية السعودية. **لسنا** لوحة مراقبة، **لسنا** مورّد عتاد، **لسنا** مركّب زراعة مائية."

**افتح:** `/` (نظرة عامة على الأسطول)
**أشِر إلى:** المؤشرات التماثلية (ISA-101) · شريط الحالة العلوي (Site · آخر مزامنة · أجهزة متصلة · تنبيهات نشطة).

### ⏱ 00:30 – 01:00 — التموضع التقني
> "كل قراءة موسومة بمصدرها (live / simulated / manual / offline) — على مستوى قاعدة البيانات ليس على مستوى الواجهة. هذه ليست زخرفة، هذه قيود NOT NULL في الـ schema."

**أظهر:** بطاقات الحساسات في `/site/site-asfan-rnd` — أشِر إلى Source Badge على كل قراءة. اضغط على pH (`live`) ثم EC (`simulated`) — اللون مختلف، الأيقونة مختلفة.

### ⏱ 01:00 – 02:30 — Golden Flow (المحور)
> "هذه هي حادثة EC التي يدور حولها كل شيء. اضغط الزر."

**اضغط:** زر "تشغيل سيناريو EC" أعلى الصفحة.
**التحول الفوري:** تنبيه P2 يظهر · بطاقة AI تفسّر (سبب + دليل + ثقة عالية) · تنبيه "يتطلّب اعتماداً بشرياً".

> "البطاقة لا تطبّق نفسها. تنتظر قراراً بشرياً. هذه الفلسفة، ليست قيداً تقنياً."

**اضغط:** "اعتماد + فتح تحكّم" في بطاقة AI.
**يفتح:** ControlConfirmModal مع 3 حقول إلزامية: سبب · تأكيد ثانوي · قفل أمان.

> "لاحظوا: لا يمكنني الضغط على 'تأكيد' حتى أملأ الحقول الثلاثة. وكل ضغطة تُسجَّل."

**املأ:** سبب ("EC مرتفع — إيقاف الجرعات لإعادة تقييم") · مؤكّد ("supervisor-shift") · قفل الأمان ON.
**اضغط:** "تأكيد وتسجيل".

### ⏱ 02:30 – 03:00 — قابلية التدقيق
**انتقل إلى:** `/logs`
> "كل أمر يُولّد سطرين: `issued` و `executed`. مع المُشغّل والمؤكّد والسبب. قابل للتصفية والتصدير CSV. هذه طبقة التدقيق التي تتوقّعها أي منظومة تشغيلية جدّية."

**اضغط:** تصدير CSV (الملف ينزّل فوراً).

### ⏱ 03:00 – 03:45 — التوطين السعودي (الخندق)
**انتقل إلى:** `/site/site-asfan-rnd`
> "هذه ثلاث بطاقات لا يستطيع منافس عالمي تقليدها فوراً:"

1. **CoolingWaterPanel:** "نفصل بصرياً مياه التبريد عن مياه الري — جوهري للزراعة المُحكَمة السعودية في الصيف."
2. **WaterEfficiency:** "مقارنة نموذجية مع خط أساس محافظ — نقول 'تستهدف تقليصاً جوهرياً' وليس رقماً مطلقاً."
3. **ROTelemetry:** "تحت كل مزرعة مائية يوجد وحدة RO. نقرأها (ضغط الدخول/الخروج، رفض الأملاح، صحة الغشاء) لأن المزارع السعودية تعتمد عليها يومياً."

### ⏱ 03:45 – 04:30 — الامتثال + التقارير
**انتقل إلى:** `/reports`
> "نولّد تقارير امتثال بنية GLOBALG.A.P. IFA. لاحظوا الـ footer: 'IFA-aligned record structure — not third-party certified'. لا ندّعي اعتماداً لم يحدث."

**اضغط:** "طباعة PDF" → تفتح نسخة قابلة للطباعة بإخلاء مسؤولية مدمج.

### ⏱ 04:30 – 05:00 — نموذج العمل + المعالم
> "نموذج الإيراد: اشتراك شهري Smart OS + SLA تشغيلي + تقارير امتثال. ليس بيع عتاد لمرّة واحدة."
>
> "بنهاية البرنامج التجريبي: نخرج بـ (1) ربط FastAPI/MQTT حيّ على بيئة البحث والتطوير، (2) demo.igarden.sa مُنشَر، (3) تكامل مُهيّأ بنيوياً مع MEWA/Naama. بدون أوهام تفوقية، بدون ادّعاءات لم تتحقّق."

**انتقل إلى:** `/demo` (وضع المستثمر) — أظهر السرد المُرقَّم 1→7.

---

## نسخة الـ 90 ثانية (المختصرة — لو كان الوقت ضيقاً)

1. **0:00** — `/` → "Smart OS — طبقة تشغيل وبيانات. كل قراءة موسومة المصدر، كل أمر مسجّل."
2. **0:15** — `/demo` → اضغط "تشغيل السيناريو الذهبي".
3. **0:30** — يظهر تنبيه EC + بطاقة AI. اعتمد البطاقة → modal تأكيد ثلاثي الحقول. أكمل.
4. **0:50** — `/logs` → "السطر هنا، مع المُشغّل والمؤكّد والسبب."
5. **1:00** — `/reports` → "تقارير امتثال — IFA-aligned — not certified."
6. **1:15** — `/site/site-asfan-rnd` → "RO + Cooling + Water Efficiency: هذه خنادقنا السعودية."
7. **1:30** — "اشتراك شهري + SLA. Smart OS من السعودية للسعودية." → close.

---

## Q&A جاهز

| السؤال | الإجابة |
|---|---|
| "هل هذا حقيقي أم محاكاة؟" | "ديمو تحقّق ميداني. بيئة البحث والتطوير تحتوي بعض حسّاسات live (pH, water temp). الباقي محاكاة موسومة بوضوح. اضغط على شفافية الديمو للقائمة الكاملة (real / simulated / next)." |
| "أين الـ AI؟" | "البطاقة تعرض: توصية + دليل + ثقة + اعتماد بشري إلزامي. لا تشغيل تلقائي. هذا قرار فلسفي وليس قصور تقني." |
| "ما الذي يميّزكم عن Priva/Argus/Netafim؟" | "لا نستبدلهم. نضع طبقة تشغيل وبيانات سعودية فوقهم: RTL عربية أولاً، تكامل MEWA-style، فصل مياه التبريد، RO telemetry. خنادق سياقية لا تقنية بحتة." |
| "متى نشاهده يعمل على عتاد فعلي؟" | "ربط FastAPI + MQTT بـ Raspberry Pi CM5 على بيئة البحث والتطوير مُجدوَل بعد انتهاء البرنامج التجريبي. يحترم STOP-GATE G2 — لا نلمس عتاداً قبل اعتماد." |
| "كم وفّرتم من المياه؟" | "في الديمو الحالي نعرض مقارنة نموذجية مع خط أساس محافظ. 'تستهدف تقليصاً جوهرياً'. لا نُعطي رقماً مطلقاً قبل تحقّق ميداني مُمتدّ." |
| "هل أنتم معتمدون من MEWA؟" | "البنية مُهيّأة للامتثال (IFA-aligned). الاعتماد الرسمي لم يحدث بعد. كل تقرير يحمل هذا الإخلاء صراحةً في الـ footer." |
| "كيف تتعامل مع انقطاع الإنترنت في الموقع؟" | "fallback states في الواجهة (offline/stale/failed) + الديمو نفسه يعمل offline من اللابتوب — مشاهدتم ذلك الآن." |

---

## خطة الطوارئ (Plan C)

- **إن فشل المتصفح:** اضغط `Ctrl+R` (تحديث) — البيانات في SQLite محلية، لا حاجة لإنترنت.
- **إن فشل المتصفح كلياً:** فيديو احتياطي مُسجَّل (موجود في `_legacy/demo-recording.mp4` — يُسجَّل قبل العرض الفعلي).
- **إن فشل اللابتوب:** ملف PDF احتياطي للسكربت + لقطات (تُولَّد بـ "طباعة PDF" من `/reports`).

---

## قبل العرض (Checklist)

- ☐ `npm install` ثم `npm run seed` نظيف.
- ☐ `npm run dev` ثم تأكّد كل المسارات: `/`, `/demo`, `/site/site-demo`, `/alerts`, `/logs`, `/reports`, `/ai`.
- ☐ شغّل Golden Flow مرّة من `/demo` ومرّة من `/` للتأكّد أن الـ AI Card تظهر سريعاً.
- ☐ افتح "شفافية الديمو" مرّة لتعرف أين الزر إن سُئلت.
- ☐ افتح PDF compliance مرّة لتعرف نموذج الإخراج.
- ☐ افتح متصفّحاً ثانياً على نفس URL لاختبار "ماذا لو شغّلت ضغطتين متوازيتين" (لا حاجة، فقط تطمين).
- ☐ أحضر شاحن اللابتوب · مزدوج إن أمكن.
- ☐ فيديو احتياطي مُحدَّث (سجّل قبل العرض بـ 24 ساعة).

---

*هذه نسخة v1.0 — مُحدَّثة 2026-05-22. أي تعديل: حدّث الإصدار والتاريخ.*
